name: scheduled_auto_change

# on:
#   schedule:
#     - cron: 0 */22 * * *
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build_master
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - run: |
        echo echo 111 >> auto_change/auto_change.md

      
      # - name: add
      #   uses: echo 111 >> auto_change/auto_change.md
      #   # with:
      #   #   name: get auto_change.md
      #   #   path: auto_change/auto_change.md
